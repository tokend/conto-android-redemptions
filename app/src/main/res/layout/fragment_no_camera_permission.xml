<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:gravity="center"
              android:background="@color/background">

    <Space android:layout_width="0dp" android:layout_height="@dimen/double_margin"/>

    <TextView
            android:id="@+id/permission_required_text_view"
            style="@style/SecondaryText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/camera_permission_required_hint"
            android:textSize="@dimen/text_size_lead"/>


    <Button
            android:id="@+id/retry_button"
            style="@style/PrimaryButton"
            android:layout_marginTop="@dimen/double_margin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_dialog"
            android:text="@string/retry"/>

    <Button
            android:id="@+id/open_settings_button"
            style="@style/SecondaryButton"
            android:layout_width="wrap_content"
            android:layout_marginTop="@dimen/half_standard_margin"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_dialog"
            android:text="@string/open_permission_settings"/>

</LinearLayout>